# AI Dynamic Island macOS Prototype

This repository hosts the dedicated macOS phase-1 prototype for the AI Dynamic Island concept.

The current scope is prototype-only. It provides a minimal AppKit + SwiftUI shell that future tasks will extend into the notch-centered island experience.

Canonical mascot assets and deterministic fixtures are copied from the approved spec documents so the repo can evolve independently while staying aligned with the design source of truth.
